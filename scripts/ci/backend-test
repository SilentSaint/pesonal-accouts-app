#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
cd "$repo_root/backend"

test -x ./gradlew || { echo "backend/gradlew is required for backend verification." >&2; exit 1; }
./gradlew --no-daemon test
