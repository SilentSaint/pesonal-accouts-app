name: automatic_expense_tracker
description: Automatic Expense Tracker Flutter Mobile & Web Client
publish_to: 'none'
version: 1.0.1+2

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  shared_preferences: ^2.2.2
  http: ^1.2.0
  google_sign_in: ^6.2.2
  google_sign_in_web: ^0.12.4
  crypto: ^3.0.3
  flutter_secure_storage: ^9.2.4
  local_auth: ^2.3.0
  web_socket_channel: ^2.4.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
